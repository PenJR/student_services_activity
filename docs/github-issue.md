# Issue: Add student-status formatter

## Description

The application should return a user-friendly label for the student's status.

## Acceptance Criteria

- "active" -> "Active Student"
- "inactive" -> "Inactive Student"
- Invalid or unexpected values should be handled safely.
- The implementation should use TypeScript types and validation.

## Notes

This issue is tracked on the feature branch `feature/student-status` and should be completed with type-safe TypeScript validation and local verification before opening the Pull Request.
